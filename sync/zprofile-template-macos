# === BladeAI Standard .zprofile for macOS ===
# Deploy: cp this file to ~/.zprofile
# NOTE: This file runs for ALL login shells (interactive + non-interactive + SSH)

# 1. Homebrew (must be first — everything else depends on it)
if [[ -f /opt/homebrew/bin/brew ]]; then
    eval "$(/opt/homebrew/bin/brew shellenv zsh)"  # Apple Silicon
elif [[ -f /usr/local/bin/brew ]]; then
    eval "$(/usr/local/bin/brew shellenv zsh)"      # Intel Mac
fi

# 2. pyenv (must be after brew, overrides system/Homebrew python)
export PYENV_ROOT="$HOME/.pyenv"
[[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"
command -v pyenv >/dev/null && eval "$(pyenv init -)"

# 3. pipx / user-local binaries
[[ -d $HOME/.local/bin ]] && export PATH="$PATH:$HOME/.local/bin"
