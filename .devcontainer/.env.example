# Machine-specific: host user UID/GID for container UID remapping
# Copy to .env and adjust: cp .env.example .env
# Find your values: echo "HOST_UID=$(id -u) HOST_GID=$(id -g)"
HOST_UID=1000
HOST_GID=1000
